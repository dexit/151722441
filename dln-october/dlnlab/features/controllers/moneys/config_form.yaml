# ===================================
#  Form Behavior Config
# ===================================

# Record name
name: Money

# Model Form Field configuration
form: $/dlnlab/features/models/money/fields.yaml

# Model Class name
modelClass: DLNLab\Features\Models\Money

# Default redirect location
defaultRedirect: dlnlab/features/moneys

# Create page
create:
    title: Create Money
    redirect: dlnlab/features/moneys/update/:id
    redirectClose: dlnlab/features/moneys

# Update page
update:
    title: Edit Money
    redirect: dlnlab/features/moneys
    redirectClose: dlnlab/features/moneys

# Preview page
preview:
    title: Preview Money