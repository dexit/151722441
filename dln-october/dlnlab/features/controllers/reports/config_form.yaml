# ===================================
#  Form Behavior Config
# ===================================

# Record name
name: Report

# Model Form Field configuration
form: $/dlnlab/features/models/report/fields.yaml

# Model Class name
modelClass: DLNLab\Features\Models\Report

# Default redirect location
defaultRedirect: dlnlab/features/reports

# Create page
create:
    title: Create Report
    redirect: dlnlab/features/reports/update/:id
    redirectClose: dlnlab/features/reports

# Update page
update:
    title: Edit Report
    redirect: dlnlab/features/reports
    redirectClose: dlnlab/features/reports

# Preview page
preview:
    title: Preview Report